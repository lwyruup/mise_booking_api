services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - booking_data:/app/data
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./data/bookings.db

volumes:
  booking_data: